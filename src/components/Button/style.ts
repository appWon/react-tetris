import styled from "styled-components";

export const Button = styled.button`
    position: absolute;
    bottom: 10rem;
    color: yellow;
    font-size: 4rem;
    font-family: "dungGeunMo";
    background: unset;
    border: unset;

    &:hover {
        cursor: pointer;
    }
`;
